# EveryMinton 작업 TODO

이 문서는 프로젝트 개선 작업의 중앙 백로그입니다. Codex 작업을 시작할 때 먼저 확인하고, 작업 완료 후 상태를 갱신합니다.

## 운영 규칙

- 실제 코드 작업은 `development` 브랜치에서 수행한다.
- 작업 단위가 완료되면 관련 TODO 상태를 갱신하고 한글 커밋 메시지로 커밋한다.
- Firestore `feedbacks` 컬렉션을 주기적으로 확인해 새 피드백을 이 문서에 반영한다.
- 사용자 피드백에서 온 항목은 원문, 출처 일정, 등록 시각, 상태를 남긴다.
- 성능/비용 관련 기존 백로그는 `docs/성능 비용 개선 TODO.md`도 함께 확인한다.

## 상태 정의

- `대기`: 아직 착수하지 않음
- `진행중`: 현재 구현 또는 검증 중
- `완료`: 코드 반영과 검증이 끝남
- `보류`: 정책 결정, 추가 정보, 외부 조건이 필요함

## 피드백 수집 현황

마지막 수집: 2026-05-31 19:38 KST

출처:
- Firestore 컬렉션: `feedbacks`
- 일정: `위너스턴 정기 모임` (`jzXGv59Dl9BQAdBgfnXq`)
- 일정 일자: 2026-05-31

## 사용자 피드백 기반 TODO

- 현재 대기/진행 항목 없음.

## 직접 요청 TODO

### P2. 참여 예정 목록 전체 선택 버튼 추가

- 상태: 대기
- 요청 내용: `참여자 탭의 참여 예정 목록에 전체 선택 버튼 추가`
- 관련 파일:
  - `app/src/components/schedule/ParticipantsTab.tsx`

작업 내용:
- 참여 예정 목록 헤더에 전체 선택 버튼을 추가한다.
- 전체 선택 시 현재 표시된 참여 예정 항목이 모두 선택되도록 한다.
- 전체 선택된 상태에서는 선택 해제 또는 선택 초기화 동작을 제공한다.
- 기존 썸네일 클릭 개별 선택 및 일괄 취소/참여 버튼과 충돌하지 않도록 한다.

검증:
- 참여 예정 목록에서 전체 선택 버튼을 누르면 참여 예정 항목이 모두 선택된다.
- 전체 선택 후 일괄 취소/참여 버튼이 기존 선택 수 기준으로 표시된다.
- 전체 선택 상태에서 선택 해제 동작이 가능하다.
- 개별 썸네일 선택/해제 동작은 유지된다.

### P3. 수동 매칭 바텀시트 선택 슬롯 영역 너비 안정화

- 상태: 대기
- 요청 내용: `수동 매칭 바텀시트에서 참여자 선택 전과 선택 후의 상단 표시 영역의 너비가 달라지는 부분 개선.`
- 관련 파일:
  - `app/src/components/schedule/ManualMatchModal.tsx`

작업 내용:
- 수동 매칭 바텀시트 상단 팀 슬롯 영역이 선택 전/후에 서로 다른 너비로 보이지 않도록 슬롯 크기와 내부 요소 배치를 안정화한다.
- 선택된 이름, 삭제 버튼, 교체/교환 아이콘이 슬롯 너비를 흔들지 않도록 고정 폭/최소 폭/오버플로우 규칙을 정리한다.
- 모바일 폭에서 팀 A/팀 B 영역과 VS 표시가 겹치거나 밀리지 않는지 확인한다.

검증:
- 참여자 선택 전과 선택 후 상단 팀 슬롯 영역의 너비가 일관되게 보인다.
- 이름이 긴 모임원을 선택해도 슬롯 영역 전체 레이아웃이 흔들리지 않는다.
- 삭제 버튼과 교체/교환 아이콘이 서로 겹치지 않는다.

### P3. 자동 매칭 미리보기 이름 카드 너비 확장

- 상태: 대기
- 요청 내용: `자동 매칭 바텀시트에서 "매칭 미리보기" 영역에 보여지는 이름 카드들의 너비가 부모의 너비를 꽉 채우도록 조정.`
- 관련 파일:
  - `app/src/components/schedule/AutoMatchModal.tsx`

작업 내용:
- 자동 매칭 바텀시트의 `매칭 미리보기` 영역에서 이름 카드가 부모 너비를 균등하게 채우도록 레이아웃을 조정한다.
- 팀별 카드와 VS 표시가 좁은 화면에서도 안정적으로 배치되도록 grid/flex 폭 규칙을 정리한다.
- 긴 이름과 급수/부가 정보가 카드 밖으로 튀어나오지 않도록 텍스트 처리 방식을 확인한다.

검증:
- 자동 매칭 미리보기의 이름 카드들이 부모 너비를 꽉 채운다.
- 모바일 폭에서 팀 카드와 VS 표시가 겹치지 않는다.
- 긴 이름이 있는 경우에도 카드 레이아웃이 깨지지 않는다.

## 완료된 작업

완료된 항목은 이력 보존을 위해 별도로 관리한다.

### 사용자 피드백 기반 완료

#### P1. 대기 탭 정렬 기준을 게임 등록 순서로 변경

- 상태: 완료
- 원문: `대기 탭 정렬 순서를 게임이 등록된 순서로`
- 피드백 ID: `o6hB2BcuUF0sOPS5FpXe`
- 등록 시각: 2026-05-31 11:27 KST
- 관련 파일:
  - `app/src/components/schedule/WaitingTab.tsx`

작업 내용:
- 대기중 게임 정렬을 현재 기준에서 `게임이 등록된 순서`로 변경한다.
- 화면의 보조 문구도 변경된 정렬 기준과 일치시킨다.
- 등록 시각이 없거나 같은 게임의 보조 정렬 기준을 기존 화면 동작과 충돌하지 않게 정한다.

검증:
- 먼저 등록된 대기중 게임이 상단에 표시되는지 확인한다.
- 새로 등록한 게임이 기존 대기중 게임 뒤에 표시되는지 확인한다.
- 등록 시각이 없거나 같은 게임도 안정적인 순서로 표시되는지 확인한다.

완료 메모:
- 2026-05-31: 게임 생성 시 `createdAt`을 저장하고, 코트 탭의 대기중 게임을 등록 시각 오름차순으로 정렬하도록 변경했다.
- 기존 `createdAt`이 없는 게임은 현재 조회 순서와 ID를 보조 기준으로 사용해 안정적인 순서를 유지한다.
- 검증: `app/`에서 `npm run lint` 통과. 로컬 `http://localhost:3000/schedule/UJYyV1wRfzl4aZZnowVA` 200 응답 확인. 기존 미사용 변수 경고 3건은 잔존.

#### P1. 대기중인 게임을 탭해서 수정 가능하게 변경

- 상태: 완료
- 원문: `대기중인 게임 누르면 수정할 수 있도록`
- 피드백 ID: `WrJgcI1aMAQXCT1byQyD`
- 등록 시각: 2026-05-31 13:19 KST
- 관련 파일:
  - `app/src/components/schedule/CourtsTab.tsx`
  - `app/src/components/schedule/ManualMatchModal.tsx`
  - `app/src/components/schedule/ScheduleDetailClient.tsx`

작업 내용:
- 현재 롱프레스 기반 수정 흐름을 일반 탭 또는 명시적인 수정 버튼으로 노출한다.
- `시작`, `취소` 버튼과 카드 탭 이벤트가 충돌하지 않도록 이벤트 전파를 제어한다.
- 모바일에서 실수로 게임이 수정 화면으로 들어가지 않도록 클릭 영역을 명확히 한다.

검증:
- 대기중인 게임 카드 탭 시 기존 4명이 선택된 수동 매칭 모달이 열린다.
- 시작/취소 버튼은 기존 동작을 유지한다.
- 조회 전용 화면에서는 수정 진입이 노출되지 않는다.

완료 메모:
- 2026-05-31: 코트 탭의 대기중인 게임 카드를 클릭/키보드 선택으로 수정할 수 있게 변경하고, 시작/취소 버튼 클릭은 카드 수정 진입과 분리했다.
- 검증: `app/`에서 `npm run lint` 통과. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 코트 수 미정 옵션 추가

- 상태: 완료
- 원문: `코트수를 정하지 않는 옵션 추가`
- 피드백 ID: `oBnshrV5GrwgkTcq763W`
- 등록 시각: 2026-05-31 14:04 KST
- 관련 파일:
  - `app/src/types/index.ts`
  - `app/src/components/ScheduleAddModal.tsx`
  - `app/src/components/schedule/CourtsTab.tsx`
  - `app/src/components/schedule/AutoMatchModal.tsx`
  - `app/src/repositories/firebase/schedule.repository.ts`

작업 내용:
- `courtCount`를 필수 숫자로 둘지, `null`을 허용할지 데이터 모델을 결정한다.
- 코트 수 미정 상태에서 코트 현황 표시, 빈 코트 계산, 게임 시작 규칙을 정의한다.
- 기존 일정 데이터와의 호환성을 유지한다.

검증:
- 코트 수를 지정한 기존 일정은 현재처럼 동작한다.
- 코트 수 미정 일정 생성/수정이 가능하다.
- 코트 수 미정 상태에서 자동 매칭과 게임 시작 UX가 어색하지 않다.

완료 메모:
- 2026-06-01: `courtCount`에 `null`을 허용하고 일정 생성/수정 모달에 `코트 수 미정` 토글을 추가했다.
- 코트 수 미정 일정은 코트 현황에서 전체 수를 `미정`으로 표시하고, 대기 게임 시작 시 가장 작은 미사용 코트 번호를 자동 배정한다.
- 자동 매칭과 일정 정보 화면도 코트 수 미정 표시를 지원한다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 로컬 `http://localhost:3000/schedule/UJYyV1wRfzl4aZZnowVA` 200 응답 확인. 기존 미사용 변수 경고 3건은 잔존.

### 직접 요청 완료

#### P2. 모바일 수동 매칭 목록 스크롤 시 바텀시트 밀림 수정

- 상태: 완료
- 요청 내용: `모바일 브라우저 환경에서 수동 매칭 바텀시트의 모임원 목록을 위로 스크롤 시, 바텀시트도 같이 내려가는 문제 수정.`
- 관련 파일:
  - `app/src/components/schedule/ManualMatchModal.tsx`

작업 내용:
- 모바일 브라우저에서 수동 매칭 바텀시트 내부 모임원 목록을 스크롤할 때 바텀시트 컨테이너가 함께 이동하지 않도록 수정한다.
- 바텀시트 전체 높이, 상단 고정 영역, 모임원 목록 스크롤 영역의 `overflow`/touch scroll 경계를 분리한다.
- iOS Safari/모바일 Chrome에서 내부 스크롤이 부모 스크롤이나 바텀시트 위치 변경으로 전파되지 않도록 `overscroll`/touch 동작을 점검한다.
- 기존 참여자 선택, 팀원 교체, 선택 슬롯 영역 동작과 충돌하지 않도록 유지한다.

검증:
- 모바일 폭에서 수동 매칭 바텀시트를 열고 모임원 목록을 위/아래로 스크롤해도 바텀시트가 내려가지 않는다.
- 목록 끝에서 추가 스크롤해도 배경 또는 바텀시트 컨테이너가 어색하게 움직이지 않는다.
- 참여자 선택, 선택 해제, 팀원 교체 버튼 동작이 유지된다.
- 데스크톱 폭에서도 수동 매칭 바텀시트 레이아웃이 깨지지 않는다.

완료 메모:
- 2026-06-05: 수동 매칭 바텀시트의 드래그 닫기 터치 핸들러를 상단 핸들 영역으로 제한하고, 모임원 목록에는 내부 스크롤 경계를 지정했다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 모임원 수정 뒤로가기 시 참여자 탭 유지

- 상태: 완료
- 요청 내용: `[메인 화면에서 일정 선택 > 참여자 탭 클릭 > 참여자 클릭하여 모임원 수정 페이지 진입 > 브라우저의 뒤로가기 버튼 클릭] 시, 일정 페이지의 참여자 탭이 보이는게 아닌, 일정페이지의 코트 탭이 노출되는 문제 수정`
- 관련 파일:
  - `app/src/app/schedule/[id]/page.tsx`
  - `app/src/app/view/schedule/[id]/page.tsx`
  - `app/src/components/schedule/ScheduleDetailClient.tsx`
  - `app/src/lib/scheduleTabs.ts`

작업 내용:
- 참여자 탭에서 모임원 수정 페이지로 이동할 때 복귀 URL 또는 히스토리에 `tab=participants` 상태가 유지되도록 한다.
- 브라우저 뒤로가기 시 일정 상세 페이지가 코트 탭으로 초기화되지 않고 참여자 탭을 표시하도록 수정한다.
- 일반 일정 페이지와 보기 전용 일정 페이지의 동작 차이를 확인한다.

검증:
- 메인 화면에서 일정 선택 후 참여자 탭으로 이동하고 모임원 수정 페이지에 진입한 뒤 브라우저 뒤로가기를 누르면 참여자 탭이 보인다.
- 코트 탭에서 진입한 흐름은 기존처럼 코트 탭 복귀 또는 의도한 기본 탭 동작을 유지한다.
- 일반 일정 페이지와 보기 전용 페이지의 탭 복귀 동작이 어긋나지 않는다.

완료 메모:
- 2026-06-05: 일정 상세 탭 상태를 URL query와 동기화하고, 참여자 탭에서 모임원 수정 페이지 진입 시 `tab=participants` 복귀 URL을 유지하도록 변경했다.
- 일반/보기 전용 일정 페이지 모두 tab query를 정규화해 초기 탭에 반영한다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 참여자 탭 목록을 참여중/참여 예정으로 재구성

- 상태: 완료
- 요청 내용: `참여자 탭에 목록은 "참여중(기존 대기중 및 게임중 )", "참여 예정" 두가지로 구분되게 변경. 참여중인 상태의 모임원은 이름 밑에 시간당 게임 수 표시.`
- 관련 파일:
  - `app/src/components/schedule/ParticipantsTab.tsx`
  - `app/src/components/schedule/ScheduleDetailClient.tsx`

작업 내용:
- 참여자 탭의 목록 구분을 `참여중`, `참여 예정` 두 가지로 정리한다.
- 기존 `waiting`, `playing` 상태 참여자는 `참여중` 목록에 함께 표시한다.
- `registered` 상태 참여자는 `참여 예정` 목록에 표시한다.
- 참여중 목록의 모임원 카드에는 이름 아래 시간당 게임 수를 표시한다.
- 기존 참여/퇴장/취소/일괄 처리 동작이 새 그룹 구조에서도 유지되도록 정리한다.

검증:
- 참여자 탭에서 `참여중`, `참여 예정` 두 목록만 표시된다.
- 기존 대기중 및 게임중 모임원이 모두 `참여중` 목록에 포함된다.
- 참여중 모임원 이름 밑에 시간당 게임 수가 표시된다.
- 기존 개별 참여/퇴장/취소와 참여 예정 일괄 처리 동작이 유지된다.

완료 메모:
- 2026-06-05: 참여자 탭의 `게임중`/`대기중` 목록을 `참여중`으로 통합하고, 참여중 카드 보조 정보에 시간당 게임 수를 표시하도록 변경했다.
- 기존 참여 예정 목록의 개별/일괄 취소 및 참여 처리와 참여중 퇴장 처리는 유지했다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 일정 종료 후 대기시간 숨김 및 시간당 게임 수 종료시간 기준 적용

- 상태: 완료
- 요청 내용: `일정 종료 시, 대기시간 더이상 미노출 되도록 수정. 시간당 게임 수는 일정 종료시간 기준 적용.`
- 관련 파일:
  - `app/src/components/schedule/CourtsTab.tsx`
  - `app/src/components/schedule/WaitingTab.tsx`
  - `app/src/components/schedule/ManualMatchModal.tsx`
  - `app/src/components/schedule/AutoMatchModal.tsx`
  - `app/src/components/schedule/ScheduleDetailClient.tsx`
  - `app/src/lib/matching.ts`
  - `app/src/lib/participantStats.ts`

작업 내용:
- 일정이 종료된 상태에서는 대기시간 표시를 숨긴다.
- 시간당 게임 수 계산은 현재 시간이 아니라 일정 종료시간을 기준으로 적용한다.
- 일정 진행 중에는 기존처럼 현재 시간을 기준으로 시간당 게임 수와 대기시간을 계산한다.
- 동일 계산 로직이 여러 화면에서 어긋나지 않도록 공통화 가능성을 검토한다.

검증:
- 종료된 일정 화면에서 대기시간 문구가 표시되지 않는다.
- 종료된 일정의 시간당 게임 수가 일정 종료시간 기준으로 계산된다.
- 진행 중인 일정에서는 기존 대기시간과 시간당 게임 수 표시가 유지된다.
- 자동/수동 매칭, 코트/대기/참여자 탭에서 표시 기준이 일관된다.

완료 메모:
- 2026-06-05: 참여자 통계 계산 유틸을 추가하고, 대기 탭/코트 탭/수동 매칭/자동 매칭의 시간당 게임 수를 일정 종료시간 기준으로 계산하도록 변경했다.
- 종료된 일정에서는 대기 탭과 수동 매칭 모임원 목록의 대기시간 문구를 숨긴다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 진행중 게임 종료 즉시 적용 및 토스트 실행 취소 추가

- 상태: 완료
- 요청 내용: `진행중인 게임의 "종료"버튼 클릭시, 팝업 노출 대신 즉각 적용 후 토스트 내에 "실행 취소" 버튼 추가. 실행 취소 버튼 클릭 시, 종료되기 전 상태로 모든 상태 복구. 일단 이러한 기능이 가능한지 분석 및 보고 후 진행.`
- 관련 파일:
  - `app/src/components/schedule/CourtsTab.tsx`
  - `app/src/components/Toast.tsx`

사전 분석:
- 구현 가능하다.
- 현재 `CourtsTab`의 게임 종료는 `endingGameId` 상태와 `ConfirmDialog`를 통해 확인 팝업을 띄운 뒤, `gameRepository.update`와 `participantRepository.updateMany`를 순차 실행한다.
- 종료 취소 복구에는 종료 직전 `Game` 상태와 해당 4명의 `Participant` 상태 스냅샷이 필요하다. 특히 `status`, `courtNumber`, `startedAt`, `endedAt`, `gamesPlayed`, `lastGameEndedAt` 값을 원복해야 한다.
- 현재 `ToastProvider`의 `showToast`는 문자열과 타입만 지원하므로, 토스트 내부 액션 버튼을 지원하도록 API 확장이 필요하다.
- 실행 취소는 토스트가 살아있는 짧은 시간 동안만 제공하는 방식이 현실적이다. 다른 사용자가 같은 게임/참여자 상태를 다시 변경한 뒤 실행 취소를 누르면 덮어쓰기 위험이 있으므로, 복구 전 현재 게임이 방금 종료된 상태인지 확인하는 방어 로직이 필요하다.

작업 내용:
- 진행중 게임의 `종료` 버튼 클릭 시 확인 팝업 없이 즉시 종료 처리한다.
- 종료 성공 후 `게임이 종료되었습니다.` 토스트에 `실행 취소` 액션 버튼을 표시한다.
- `실행 취소` 클릭 시 종료 직전 게임 상태와 4명의 참여자 상태를 복구한다.
- 실행 취소 성공/실패 토스트를 표시하고, 실패 시 현재 상태를 유지한다.
- 기존 진행중 게임 롱프레스 취소, 대기 게임 시작/취소 동작과 충돌하지 않도록 이벤트 흐름을 유지한다.

검증:
- 진행중 게임의 `종료` 버튼을 누르면 확인 팝업 없이 즉시 종료된다.
- 종료 성공 토스트에 `실행 취소` 버튼이 표시된다.
- `실행 취소` 버튼을 누르면 게임이 다시 진행중 상태로 돌아오고, 코트 번호/시작시간/종료시간이 종료 전 상태로 복구된다.
- 해당 게임 참여자 4명의 `status`, `gamesPlayed`, `lastGameEndedAt`이 종료 전 상태로 복구된다.
- 토스트 만료 후에는 실행 취소를 할 수 없다.
- 실행 취소 실패 시 오류 토스트가 표시되고 앱이 깨지지 않는다.

완료 메모:
- 2026-06-04: 진행중 게임 종료 확인 팝업을 제거하고, 종료 직전 게임/참여자 스냅샷을 기반으로 토스트 `실행 취소` 액션에서 상태를 복구하도록 변경했다.
- 실행 취소 전 현재 게임이 방금 종료된 동일 게임인지 확인해 이후 변경을 덮어쓰지 않도록 했다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 대기 게임 카드의 게임중 시간 표시 위치 조정

- 상태: 완료
- 요청 내용: `대기중인 게임 목록에 게임중 표시는 해당 이름 카드 아래에 표시하고, 이름 카드 아래에는 해당 이름의 게임 시간만 "게임중 n분" 형태로 표시`
- 관련 파일:
  - `app/src/components/schedule/CourtsTab.tsx`

작업 내용:
- 대기중인 게임 카드 하단에 모아 표시하던 게임중 시간 배지를 제거한다.
- 게임중인 모임원별로 해당 이름 카드 바로 아래에 `게임중 n분`만 표시한다.
- 게임중이 아닌 모임원 카드 아래에는 별도 게임중 시간을 표시하지 않는다.

검증:
- 대기중인 게임 목록에서 게임중인 모임원의 이름 카드 아래에만 `게임중 n분`이 표시된다.
- 대기중인 게임 카드 하단에는 이름 포함 게임중 시간 배지가 표시되지 않는다.

완료 메모:
- 2026-06-02: 대기중인 게임 카드 하단에 모아 보여주던 게임중 시간 배지를 제거하고, 각 이름 카드 바로 아래에 `게임중 n분`만 표시하도록 변경했다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 로컬 `http://localhost:3000/schedule/UJYyV1wRfzl4aZZnowVA`에서 이름 없이 `게임중 n분`이 각 이름 카드 아래에 표시되는 것을 확인. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 코트 수 미지정 코트 현황 정렬을 게임 시작 순서로 변경

- 상태: 완료
- 요청 내용: `코트 수 미지정인 경우 코트 현황에 보여지는 게임의 순서가 게임이 시작된 순서로 표시되도록 변경`
- 관련 파일:
  - `app/src/components/schedule/CourtsTab.tsx`

작업 내용:
- 코트 수가 미지정인 일정의 코트 현황은 진행중 게임을 `startedAt` 오름차순으로 표시한다.
- 코트 수가 지정된 일정은 기존처럼 코트 번호 기준 표시를 유지한다.
- 시작 시각이 없거나 같은 게임도 안정적인 순서로 표시한다.

검증:
- 코트 수 미지정 일정에서 먼저 시작한 진행중 게임이 먼저 표시된다.
- 코트 수 지정 일정의 코트 현황 정렬은 기존과 동일하다.

완료 메모:
- 2026-06-02: 코트 수 미지정 일정의 코트 현황은 진행중 게임을 `startedAt` 오름차순으로 정렬해 먼저 시작한 게임부터 표시하도록 변경했다.
- 코트 수 지정 일정은 기존처럼 코트 번호 순서와 빈 코트 표시를 유지한다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 로컬 `http://localhost:3000/schedule/UJYyV1wRfzl4aZZnowVA`에서 오래 진행된 게임이 최근 시작 게임보다 먼저 표시되는 것을 확인. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 대기 게임 카드의 게임중 모임원 진행 시간 표시

- 상태: 완료
- 요청 내용: `대기중인 게임에 게임중인 모임원이 있을 경우, 해당 모임원의 게임 진행 시간을 카드 하단에 표시하고, 대기 게임 카드의 "게임중인 인원 포함" 문구와 "등록된 순서대로 표시" 문구를 제거`
- 관련 파일:
  - `app/src/components/schedule/CourtsTab.tsx`

작업 내용:
- 대기중인 게임 카드에 포함된 게임중 모임원의 현재 진행중 게임 시간을 카드 하단에 표시한다.
- 플레이어 칩 내부에는 진행 시간을 넣지 않는다.
- 대기 게임 카드의 `게임중인 인원 포함` 경고 문구를 제거한다.
- 대기중인 게임 헤더의 `등록된 순서대로 표시` 문구를 제거한다.
- 진행중인 게임 20분 초과 자동 종료는 이번 구현 범위에서 제외한다.

검증:
- 게임중인 모임원이 포함된 대기 게임 카드 하단에 해당 모임원의 게임 진행 시간이 표시된다.
- 플레이어 칩 내부 표시는 기존 이름/급수/시간당 게임 횟수 중심 표시를 유지한다.
- 대기 게임 카드에서 `게임중인 인원 포함`, `등록된 순서대로 표시` 문구가 보이지 않는다.
- 자동 매칭 모달의 `게임중 인원 포함` 옵션은 유지된다.

완료 메모:
- 2026-06-02: 대기중인 게임 카드 하단에 포함된 게임중 모임원의 진행 시간을 `이름 게임중 n분` 형태로 표시하도록 변경했다.
- 대기 게임 카드의 `게임중인 인원 포함` 경고 문구와 `등록된 순서대로 표시` 보조 문구를 제거하고, 자동 매칭 모달의 `게임중 인원 포함` 옵션은 유지했다.
- 진행중인 게임 20분 초과 자동 종료는 이번 구현 범위에서 제외했다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit` 통과. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 수동 매칭 화면의 게임중 모임원 진행 시간 표시

- 상태: 완료
- 요청 내용: `수동 매칭 화면에서 게임중인 모임원은 현재 진행중인 게임의 진행시간을 표시`
- 관련 파일:
  - `app/src/components/schedule/ManualMatchModal.tsx`

작업 내용:
- 수동 매칭 참여자 목록에서 `playing` 상태인 모임원이 현재 진행중인 게임에 포함되어 있으면 진행 시간을 표시한다.
- 진행 시간은 해당 모임원이 포함된 `in_progress` 게임의 `startedAt` 기준으로 계산한다.
- 게임중 필터와 전체 필터 모두에서 동일한 진행 시간 표시를 유지한다.
- 진행중 게임을 찾을 수 없거나 `startedAt`이 없는 경우에는 진행 시간 표시를 생략하거나 안정적인 대체 문구를 사용한다.

검증:
- 수동 매칭 화면에서 게임중인 모임원 카드에 `게임중 n분` 형태의 진행 시간이 표시된다.
- 게임중이 아닌 모임원 카드에는 진행 시간이 표시되지 않는다.
- `게임중` 필터와 `전체` 필터에서 진행 시간 표시가 동일하게 동작한다.
- 기존 선택/해제, 4명 선택, 게임 수정/생성 동작은 유지된다.

완료 메모:
- 2026-06-03: 수동 매칭 참여자 목록에서 `playing` 상태 모임원이 포함된 진행중 게임의 `startedAt` 기준으로 `게임중 n분`을 표시하도록 변경했다.
- 진행중 게임을 찾을 수 없거나 시작 시간이 없는 경우 기존 `게임중` 상태 표시만 유지하고 진행 시간은 생략한다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit --incremental false` 통과. 로컬 `/`, `/schedule/UJYyV1wRfzl4aZZnowVA` 200 응답 확인. 기존 미사용 변수 경고 3건은 잔존.

#### P2. 수동 매칭 선택 팀원 교체 기능 추가

- 상태: 완료
- 요청 내용: `수동 매칭 화면에서 4명이 선택된 상태에서 선택된 모임원끼리 팀원을 교체하는 기능 추가`
- 관련 파일:
  - `app/src/components/schedule/ManualMatchModal.tsx`

작업 내용:
- 수동 매칭 화면에서 4명이 모두 선택된 상태일 때 선택 슬롯별 교체 진입 버튼을 노출한다.
- 기본 교체 방식은 선택된 이름의 우측 하단, 기존 X 버튼 아래에 `교체` 버튼을 표시하는 방식으로 구현한다.
- 특정 슬롯의 `교체` 버튼을 누르면 상대팀 슬롯에만 `교환` 버튼을 활성화하거나 노출한다.
- 상대팀원의 `교환` 버튼을 누르면 처음 선택한 슬롯과 해당 상대팀 슬롯의 위치를 서로 바꾼다.
- 교체 모드 진입 후 취소/닫기/다른 슬롯 선택 시 교체 대기 상태가 어색하게 남지 않도록 정리한다.
- 구현 중 버튼 방식보다 명확한 UX가 확인되면, 대체 방식 적용 전 사용자 컨펌을 받는다.

검증:
- 4명이 선택되지 않은 상태에서는 교체 진입 버튼이 노출되지 않거나 비활성화된다.
- 4명이 선택된 상태에서 특정 이름의 `교체` 버튼을 누르면 상대팀 이름에만 `교환` 버튼이 표시된다.
- 상대팀의 `교환` 버튼을 누르면 두 모임원의 슬롯 위치가 바뀐다.
- 같은 팀끼리는 교환할 수 없다.
- 기존 X 버튼 삭제, 참여자 선택/해제, 게임 생성/수정 동작은 유지된다.

완료 메모:
- 2026-06-03: 4명이 모두 선택된 수동 매칭 슬롯에 `교체` 버튼을 표시하고, 교체 모드에서는 선택 슬롯의 `취소` 버튼과 상대팀 슬롯의 `교환` 버튼만 표시하도록 구현했다.
- 상대팀 슬롯의 `교환` 버튼을 누르면 선택 슬롯 배열의 위치를 서로 바꾸고, 교체 대기 상태를 초기화한다.
- 검증: `app/`에서 `npm run lint`, `npx tsc --noEmit --incremental false` 통과. 기존 미사용 변수 경고 3건은 잔존. 브라우저에서 상대팀에만 `교환` 표시, 같은 팀 교환 불가, 교환 실행, 교체 취소 동작 확인.

#### P2. 참여 예정 목록 일괄 선택 및 참여/취소 버튼 추가

- 상태: 완료
- 요청 내용: `참여 예정 목록에서 항목의 썸네일(원형 배경) 클릭시 아이템이 체크되며 체크된 아이템들을 한번에 취소/참여할 수 있는 버튼을 "참여 예정(n명)" 텍스트 우측에 표시`
- 관련 파일:
  - `app/src/components/schedule/ParticipantsTab.tsx`

작업 내용:
- 참여 예정 목록 항목의 썸네일(원형 배경)을 클릭하면 해당 항목의 선택 상태가 토글되도록 한다.
- 선택된 참여 예정 항목 수에 따라 `참여 예정(n명)` 텍스트 우측에 일괄 `취소`/`참여` 버튼을 표시한다.
- 개별 항목의 기존 `참여`/`취소` 동작과 일괄 선택 동작이 충돌하지 않도록 클릭 영역과 이벤트 전파를 정리한다.
- 선택된 항목들의 참여 상태 변경을 한 번에 처리하고, 처리 후 선택 상태를 초기화한다.

검증:
- 썸네일(원형 배경) 클릭 시 해당 참여 예정 항목의 체크 상태가 토글된다.
- 이름 영역 클릭 시 선택 상태가 토글되지 않는다.
- 체크된 항목이 있을 때 `참여 예정(n명)` 우측에 일괄 처리 버튼이 표시된다.
- 여러 항목을 선택한 뒤 일괄 `취소`/`참여` 동작이 모두 반영된다.
- 개별 항목의 기존 버튼 동작은 유지된다.

완료 메모:
- 2026-06-01: 참여 예정 항목의 프로필 영역 선택 토글과 헤더 우측 일괄 `취소`/`참여` 버튼을 추가했다.
- 일괄 참여는 `updateMany`로 대기중 전환과 `joinedAt` 기록을 함께 처리하고, 일괄 취소는 선택된 참여자 문서를 삭제한다.
- 검증: `app/`에서 `npm run lint` 통과. 로컬 `http://localhost:3000/schedule/UJYyV1wRfzl4aZZnowVA` 200 응답 확인. 기존 미사용 변수 경고 3건은 잔존.
- 2026-06-01 수정: 선택 토글 클릭 영역을 이름/정보 영역이 아닌 썸네일(원형 배경)으로 제한했다.

## 성능/비용 개선 TODO

상세 항목은 `docs/성능 비용 개선 TODO.md`를 기준으로 관리한다.

현재 주요 항목:
- P1. 일정 상세 화면 전체 재조회 줄이기
- P1. 완료된 게임 전체 조회 분리
- P2. 홈/회원 목록 무제한 조회 제한
- P2. 자동 매칭 계산 비용 줄이기
- P2. 참여자 추가 모달 검색/정렬 최적화
- P3. 빌드 네트워크 의존성 제거 검토
- P3. 정적 검사 경고 정리

## 다음 추천 작업

1. 참여 예정 목록 전체 선택 버튼 추가
2. 수동 매칭 바텀시트 선택 슬롯 영역 너비 안정화
3. 자동 매칭 미리보기 이름 카드 너비 확장
4. 일정 상세 화면 전체 재조회 줄이기
5. 완료된 게임 전체 조회 분리
6. 자동 매칭 계산 비용 줄이기
7. 참여자 추가 모달 검색/정렬 최적화
